#include <stdio.h>

/**
   * main - prints name of file.
    * @argc: size of argv
     * @argv: arguments entered
       * Return: 0
        */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	argc = argc;

	return (0);
}
