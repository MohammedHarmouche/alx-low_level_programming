#include <stdio.h>

/**
   *main- prints the number of argument entred
    *@argc: the number of arguments
     *@argv: arguments entered
       *Return: 0
	*/

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	argv = argv;

	return (0);
}
